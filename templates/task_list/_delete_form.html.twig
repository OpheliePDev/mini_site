<form method="post" action="{{ path('app_task_list_delete', {'id': task_list.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette liste ?');" class="inline-block">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task_list.id) }}">
    <button class="btn-base btn-danger">🗑️ Supprimer</button>
</form>
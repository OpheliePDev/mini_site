<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Mon site{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
</head>
<body>
    
    <header>
        <nav class="navbar navbar-expand-lg">
            <div class="container">
                <a class="navbar-brand" href="{{ path('app_home') }}">{{ site.name }}</a>
                <div class="collapse navbar-collapse">
                    <nav class="navbar-nav">
                        <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
                        <a class="nav-link" href="{{ path('about') }}">À propos</a>
                        <a class="nav-link" href="{{ path('contact') }}">Contact</a>
                        <a class="nav-link" href="{{ path('app_task_index') }}">ToDoList</a>
                    </nav>
                </div>
            </div>
        </nav>
    </header>

    <main class="container">
        {% block body %}{% endblock %}
    </main>

    <footer>
        {% if site.logo %}
            <img src="{{ site.logo }}" alt="{{ site.name }}" class="logo">
        {% endif %}
        <p>© {{ site.name }} - {{ "now"|date("Y") }}</p>
        <p>Contact : <a href="mailto:{{ site.email }}">{{ site.email }}</a></p>
    </footer>
</body>
</html>
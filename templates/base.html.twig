<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Mon site{% endblock %}</title>
    <meta name="description" content="{% block meta_description %}Application Symfony éco-responsable et minimaliste{% endblock %}">
    {# Feuille de style (CSS simple ou build Tailwind plus tard) #}
    <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
</head>
<body>
    <header class="header">
        <nav class="navbar">
            <section class="nav-container">
                <a href="{{ path('app_home') }}" class="nav-brand">🌱 EcoDevLab - Symfony</a>
                <ul class="nav-menu">
                    <li><a href="{{ path('app_home') }}">Accueil</a></li>
                    <li><a href="{{ path('about') }}">À propos</a></li>
                    <li><a href="{{ path('app_task_list_index') }}">ToDoList</a></li>
                    <li><a href="{{ path('contact') }}">Contact</a></li>
                </ul>
            </section>
        </nav>
    </header>

    <main class="main-content">
        {% block body %}{% endblock %}
    </main>

    <footer class="footer">
        {% if site.logo %}
            <figure class="footer-logo">
                <img src="{{ site.logo }}" alt="{{ site.name }}" class="footer-logo__img">
            </figure>
        {% endif %}
        <p>© {{ site.name }} - {{ "now"|date("Y") }}</p>
        <p>
            Contact : 
            <a href="mailto:{{ site.email }}">{{ site.email }}</a>
        </p>
    </footer>
</body>
</html>

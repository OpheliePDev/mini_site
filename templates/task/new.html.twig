{% extends 'base.html.twig' %}

{% block title %}Créer une tâche{% endblock %}

{% block body %}
    <h1 class="mb-4">Créer une tâche pour la liste {{ task_list.name }}</h1>
    <section class="card p-4 shadow-sm border-0">
        {{ include('task/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    </section>
    <div class="d-flex justify-content-between align-items-center mt-4">
        <a href="{{ path('app_task_list_index') }}" class="btn btn-outline-secondary">⬅️ Retour à la liste</a> 
    </div>
{% endblock %}
